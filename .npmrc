# pnpm configuration optimized for Docker builds

# Use copying instead of hard-linking for better Docker compatibility
# This prevents broken links when copying between Docker stages
package-import-method=copy

# Strict peer dependencies for consistent builds
strict-peer-dependencies=true

# Shameless hoist disabled for predictable module resolution
shamefully-hoist=false
